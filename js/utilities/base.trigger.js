import { $, $$ } from './base.dom';

class Trigger {

  constructor(document) {
    this.document = document;
    this.addToggleListener();
  }

  addOpenListener() {
  }

  addCloseListener() {
  }

  addToggleListener() {
  }
}

export { Trigger }
