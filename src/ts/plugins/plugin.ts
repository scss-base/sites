export class Plugin {
  private element: HTMLElement;
  private elementId: string;

  /**
   *
   * @param {HTMLElement} element - Element object to use for the modal.
   */
  constructor(element: HTMLElement) {
    this.element = element;
    this.elementId = element.id;
  }

  set
}
