import { Dropdown, DropdownMenu, Modal, Toggler, Tooltip } from '../plugin';
import { MediaQuery } from '../utility';
import { $ } from '../helper';

const Base = {
  $,
  Dropdown,
  DropdownMenu,
  MediaQuery,
  Modal,
  Toggler,
  Tooltip,
};

window['Base'] = Base;
