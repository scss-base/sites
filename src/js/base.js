import { Dropdown, DropdownMenu, Modal, Toggler, Tooltip } from './plugins';
import { $ } from './helper';

const Base = window.Base = {
  $,
  Dropdown,
  DropdownMenu,
  Modal,
  Toggler,
  Tooltip,
};
