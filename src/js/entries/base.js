import {Dropdown, DropdownMenu, Modal, Toggler, Tooltip} from '../plugin';
import {$} from '../helper';

const Base = {
  $,
  Dropdown,
  DropdownMenu,
  Modal,
  Toggler,
  Tooltip,
};
window['Base'] = Base;
